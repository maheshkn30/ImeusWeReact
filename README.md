<!-- first
install all necessary packages -> npm i

.Second
npm run dev <-- to run the applciation

necessary install
change code in package.json
 "start:server": "node ./src/PersonModel.js",
    "dev": "concurrently \"npm run start:react\" \"npm run start:server\""

    addtional install are
   1. npm install cors
   2. npm install concurrently --save-dev
 -->
